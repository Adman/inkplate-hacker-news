#pragma once

/**
 * Template for hn/secrets.h (copy next to this sketch; see README).
 * Wi‑Fi + NVIDIA API key only — URL/model stay in hn.ino.
 */

char ssid[] = "YOUR_WIFI_SSID";
char pass[] = "YOUR_WIFI_PASSWORD";

static const char kNvidiaApiKey[] = "YOUR_NVIDIA_API_KEY";
